# Re-export generics

These generics are re-exported from the generics package for
convenience.

## Usage

``` r
tidy(x, ...)

glance(x, ...)

augment(x, ...)
```

## Arguments

- x, ...:

  Passed to the generic.
