% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlxs-lm.R
\name{mlxs_lm}
\alias{mlxs_lm}
\title{MLX-backed linear regression}
\usage{
mlxs_lm(
  formula,
  data,
  subset,
  weights,
  na.action = stats::na.exclude,
  rank_tol = NULL
)
}
\arguments{
\item{formula}{Model formula.}

\item{data}{Optional data frame, tibble, or environment containing the
variables in the model.}

\item{subset}{Optional expression for subsetting observations.}

\item{weights}{Optional non-negative observation weights.}

\item{na.action}{How to handle missing values.}

\item{rank_tol}{Optional relative tolerance used to detect rank-deficient
systems. \code{NULL} uses the package default, which varies by dtype and is
1e-6 for float32 matrices. Set to \code{FALSE} to skip rank checks entirely.
Note that higher numbers indicate \emph{lower} tolerance.}
}
\value{
An object of class \code{c("mlxs_lm", "mlxs_model")} containing
components similar to an \code{"lm"} fit, along with MLX intermediates stored in
the \code{mlx} element.
Note that MLX currently operates in single precision, so fitted values and
diagnostics may differ from \code{stats::lm()} at around the 1e-6 level. Unlike
\code{\link[stats:lm]{stats::lm()}}, rank-deficient model matrices are rejected rather than fit
with aliased coefficients.
}
\description{
Fit a linear model via QR decomposition using MLX arrays on Apple Silicon
devices. The interface mirrors \code{\link[stats:lm]{stats::lm()}} for the common arguments.
}
\examples{
fit <- mlxs_lm(mpg ~ cyl + disp, data = mtcars)
coef(fit)
}
