% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coordinate-descent-general.R
\name{coordinate_descent}
\alias{coordinate_descent}
\title{General Coordinate Descent with L1 Regularization}
\usage{
coordinate_descent(
  loss_fn,
  beta_init,
  lambda = 0,
  grad_fn = NULL,
  lipschitz = NULL,
  batch_size = NULL,
  compile = FALSE,
  max_iter = 1000,
  tol = 1e-06
)
}
\arguments{
\item{loss_fn}{Function(beta) -> scalar loss (MLX tensor). Should be smooth.}

\item{beta_init}{Initial beta (p x 1 MLX tensor)}

\item{lambda}{L1 penalty parameter (scalar)}

\item{grad_fn}{Optional pre-computed gradient function. If NULL, uses mlx_grad(loss_fn)}

\item{lipschitz}{Optional Lipschitz constants for each coordinate (length p vector).
If NULL, uses backtracking line search.}

\item{batch_size}{Number of coordinates to update simultaneously (1 = sequential, p = full batch)}

\item{compile}{Whether to compile the update step}

\item{max_iter}{Maximum iterations}

\item{tol}{Convergence tolerance}
}
\value{
List with beta, n_iter, converged
}
\description{
Solves: min_beta f(beta) + lambda * ||beta||_1
where f is a smooth differentiable loss function.
}
