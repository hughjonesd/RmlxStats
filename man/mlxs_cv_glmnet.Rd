% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlxs-cv-glmnet.R
\name{mlxs_cv_glmnet}
\alias{mlxs_cv_glmnet}
\title{Cross-validated MLX elastic net regression}
\usage{
mlxs_cv_glmnet(
  x,
  y,
  weights = NULL,
  offset = NULL,
  lambda = NULL,
  type.measure = c("default", "mse", "deviance", "class", "mae", "auc", "C"),
  nfolds = 10,
  foldid = NULL,
  alignment = c("lambda", "fraction"),
  grouped = TRUE,
  keep = FALSE,
  parallel = FALSE,
  gamma = c(0, 0.25, 0.5, 0.75, 1),
  relax = FALSE,
  trace.it = 0,
  family = mlxs_gaussian(),
  ...
)
}
\arguments{
\item{x}{Numeric matrix of predictors (observations in rows).}

\item{y}{Numeric response vector.}

\item{weights}{Optional observation weights. Currently unsupported.}

\item{offset}{Optional offset. Currently unsupported.}

\item{lambda}{Optional decreasing lambda sequence. If \code{NULL}, the full-data
fit chooses the path and the same path is reused inside each fold.}

\item{type.measure}{Loss used to score the holdout predictions.}

\item{nfolds}{Number of folds.}

\item{foldid}{Optional integer vector giving the fold assignment for each
observation.}

\item{alignment}{Alignment mode. Only \code{"lambda"} is currently supported.}

\item{grouped}{Should cross-validation be aggregated fold-by-fold? Only
\code{TRUE} is currently supported.}

\item{keep}{Should out-of-fold predictions be stored?}

\item{parallel}{Logical. Parallel refits are currently unsupported.}

\item{gamma, relax}{Relaxed fits are currently unsupported.}

\item{trace.it}{Progress tracing. Currently unsupported.}

\item{family}{MLX-aware family object, e.g. \code{\link[=mlxs_gaussian]{mlxs_gaussian()}} or
\code{\link[=mlxs_binomial]{mlxs_binomial()}}.}

\item{...}{Additional arguments passed to \code{\link[=mlxs_glmnet]{mlxs_glmnet()}}, such as \code{alpha},
\code{nlambda}, \code{lambda_min_ratio}, \code{standardize}, \code{intercept}, \code{maxit}, and
\code{tol}.}
}
\value{
An object of class \code{mlxs_cv_glmnet}.
}
\description{
Cross-validation wrapper around \code{\link[=mlxs_glmnet]{mlxs_glmnet()}} that mirrors the core
\code{glmnet::cv.glmnet()} workflow for the families currently supported by
\code{mlxs_glmnet()}.
}
\details{
The full-data fit defines a master lambda path. Each fold is then refit on
the same lambda values and scored on its holdout set.

Current limitations relative to \code{glmnet::cv.glmnet()}:
\itemize{
\item only Gaussian and binomial families are supported
\item \code{weights}, \code{offset}, \code{alignment != "lambda"}, \code{grouped = FALSE},
\code{parallel = TRUE}, \code{relax = TRUE}, and non-zero \code{trace.it} are not
implemented
\item \code{type.measure = "auc"} and \code{type.measure = "C"} are not implemented
}
}
